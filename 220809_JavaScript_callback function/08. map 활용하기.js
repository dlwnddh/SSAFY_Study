const arr = [1, 2, 3, 4, 5];

const powArr = arr.map((num) => num * num);
console.log(powArr);

const arr2 = ["a", "bcd", "ef", "g"];

const lengthArr = arr2.map((num) => num.length);
console.log(lengthArr);
